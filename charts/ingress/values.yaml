ingress:
  name: ingress-service
  replicaCount: 1
  annotations:
    class: nginx
  hosts:
    - host: yellowtaxi.com #todo: replace with custom domain
      paths:
        - path: /
          backend:
            serviceName: yellow-taxi-api
            servicePort: 80
