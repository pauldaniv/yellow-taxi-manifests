app:
  name: yellow-taxi-api
  group: backend
  replicaCount: 1
  container:
    image:
      repository: "aws.ecr.com"
      name: taxi-trip-api
      version: latest6
      pullPolicy: Never

    port: 8080
    config: []
    env:
      - key: key
        value: value
  service:
    type: ClusterIP
    port: 80
