{{- range .Values.secret }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .name }}
  namespace: external-secrets

spec:
  refreshInterval: 1m
  secretStoreRef:
    name: yt-secret-store
    kind: SecretStore
  target:
    name: {{ .secretTargetKey }}
{{/*    creationPolicy: Owner*/}}
  data:
    - secretKey: "{{ .secretTargetKey }}-secret-key"
      remoteRef:
        key: {{ .secretSourceKey }}#AWS secret name
{{- end }}