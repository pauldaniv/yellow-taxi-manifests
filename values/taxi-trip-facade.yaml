app:
  name: facade
  container:
    image:
      repository: me.local
      name: yt-facade
      versionTag: latest
    port: 8081
    healthCheckPath: /v1/health_check
    env:
      KEY: value
      KAFKA_HOSTNAME: kafka.kafka
      REDIS_HOSTNAME: redis.redis
