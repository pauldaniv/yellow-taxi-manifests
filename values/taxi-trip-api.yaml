app:
  name: api
  container:
    image:
      repository: me.local
      name: yt-api
      versionTag: latest
    env:
      KEY: value
      KAFKA_HOSTNAME: kafka.kafka
      DB_HOST: postgres.db
      DB_PORT: 5432
