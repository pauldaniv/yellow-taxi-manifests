app:
  name: api
  container:
    image:
      name: taxi-trip-api
      versionTag: 9fe8a5a
    env:
      KEY: value
      KAFKA_HOSTNAME: kafka.kafka
      DB_HOST: postgres.db
      DB_PORT: 5432
