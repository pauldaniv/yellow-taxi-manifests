app:
  name: api
  container:
    image:
      name: yt-api
      versionTag: latest
      pullPolicy: IfNotPresent
    env:
      KEY: value
      KAFKA_HOSTNAME: kafka.kafka
      DB_HOST: postgres.db
      DB_PORT: 5432
